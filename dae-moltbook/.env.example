# DAE Moltbook Agent — Environment Configuration
# Copy this file to .env and fill in your keys.
# NEVER commit .env to version control.
#
# Created by smaxforn

# ── REQUIRED ──────────────────────────────────────────────

# Your Moltbook agent API key (from registration/claim process)
MOLTBOOK_API_KEY=moltbook_sk_your_key_here

# Your LLM provider API key
LLM_API_KEY=sk-your-key-here

# ── LLM PROVIDER ─────────────────────────────────────────

# Which LLM backend to use: claude | openai | grok | gemini
LLM_PROVIDER=claude

# Optional: override default model
# Claude:  claude-sonnet-4-20250514 (default), claude-opus-4-6
# OpenAI:  gpt-4o (default), gpt-4o-mini
# Grok:    grok-3 (default)
# Gemini:  gemini-2.0-flash (default)
# LLM_MODEL=claude-sonnet-4-20250514

# ── AGENT IDENTITY ───────────────────────────────────────

# Your agent's name on Moltbook
DAE_AGENT_NAME=dae-agent

# Which submolt to post in
MOLTBOOK_SUBMOLT=general

# ── BEHAVIOR ─────────────────────────────────────────────

# How often to poll Moltbook for new posts (milliseconds)
POLL_INTERVAL_MS=30000

# Exchanges before creating a new memory episode
EPISODE_THRESHOLD=5

# Conversation window size (recent messages sent to LLM)
CONVERSATION_WINDOW=5

# Max response length in tokens
MAX_RESPONSE_LEN=2000

# Log a heartbeat every N polls (0 to disable)
HEARTBEAT_EVERY=50

# ── STORAGE ──────────────────────────────────────────────

# Where to persist DAE state (manifold, episodes, conversation history)
# DAE_STATE_DIR=./.dae-state

# ── ADVANCED ─────────────────────────────────────────────

# Override Moltbook API base URL (if self-hosted or staging)
# MOLTBOOK_API_URL=https://www.moltbook.com/api/v1
